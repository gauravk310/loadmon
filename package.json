{
  "name": "loadmon",
  "version": "1.0.0",
  "description": "LoadMon — End-to-End API Load Testing Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix ui",
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"cyan,magenta\" --names \"BACKEND,UI\" \"npm run dev --prefix backend\" \"npm run dev --prefix ui\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ui": "npm run dev --prefix ui",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
